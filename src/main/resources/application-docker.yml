spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://postgres:5432/msa
    username: msa
    password: msa

  data:
    redis:
      host: redis
      port: 6379
